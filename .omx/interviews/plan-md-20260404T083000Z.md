# Deep Interview Transcript Summary: plan-md

- Profile: standard
- Context type: brownfield
- Final ambiguity: 19%
- Threshold: 20%
- Context snapshot: `.omx/context/plan-md-20260404T082700Z.md`

## Repository grounding
- `plan.md` is the only repo file currently describing the Runpod / ComfyUI / S3 / LTX initiative.
- Existing brownfield surface: `apps/web`, `apps/server`, `packages/db`, `packages/env`, `packages/ui`, `packages/auth`, `packages/config`.
- No existing Runpod / ComfyUI implementation was found in the repository search.

## Clarified intent
Build an internal operator product for diffusion-based media generation. The near-term goal is stable execution of ComfyUI workflows through serverless Runpod, not perfect consistency or model training.

## Key clarified points
- Target users: internal operators.
- Core use cases: generate photos/videos, create characters, run reusable action scenarios.
- Future phase: identity consistency via LoRA training.
- MVP non-goals: LoRA training, external self-serve product, auth, advanced moderation/analytics/editor features.
- MVP success: operator creates a scenario with workflow + prompt, reruns it with different input images, and the system returns outputs; a human moderator selects usable results.
- Scenario meaning: the scenario primarily encodes an action/motion pattern (for example jump, rotate around self, etc.).
- Decision boundaries: implementation details may be chosen autonomously.
- Infra simplification: start with a minimal working setup first rather than immediate multi-datacenter replication.

## Condensed transcript
1. Q: What makes this project necessary?
   A: Media generation with diffusion models.
2. Q: What should distinguish this from just running ComfyUI manually?
   A: Internal operator product for photo/video generation, character creation, future same-face consistency.
3. Q: What is out of scope for V1?
   A: LoRA training later; current focus is stable ComfyUI workflow execution via serverless Runpod; admin UI only; no auth yet.
4. Q: What can OMX decide autonomously?
   A: Everything in MVP implementation; user will later provide Runpod access through keys/MCP.
5. Q: What E2E flow proves MVP works?
   A: User creates a scenario with workflow and prompt, then reruns it with different images through the same workflow.
6. Q: What should remain stable across reruns?
   A: The scenario encodes the action/motion pattern.
7. Q: What counts as acceptable MVP output quality?
   A: Any 10 resulting videos are acceptable; moderator reviews and selects what is useful.
8. Q: Must MVP include multi-datacenter replication immediately?
   A: No; start with the minimal working version first.

## Pressure pass
- Revisited earlier claim about “stable generation”.
- Clarified that scenario stability for MVP is about reliably applying the intended action/motion workflow, not about strong visual identity consistency across all outputs.
