{
  "name": "mcp",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@generator/auth": "workspace:*",
    "@generator/db": "workspace:*",
    "@generator/debug-tools": "workspace:*",
    "hono": "^4.8.2",
    "kafkajs": "^2.2.4",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@generator/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "^5"
  }
}
